# Apples

A true-black field of spinning apples. Some turn clockwise, some the other way. A few are cut in half. A few have a slice taken through them.

`+` / `−` changes how many there are. Click an apple to reverse it.
